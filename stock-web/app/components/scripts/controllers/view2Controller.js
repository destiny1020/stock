/* global console: true */
'use strict';
var app = angular.module('hgsys');

// view2 controller
app.controller('View2Ctrl', ['$scope',
    function($scope) {
        console.log('view 2 loaded');
    }
]);